interface:
  display_name: "Recursive Design Loop"
  short_description: "Recursive deployed-screen UX and performance polish with protected-preview and manager/admin readiness handling"
  default_prompt: "Use $recursive-design-loop to inspect a specific deployed screen for the current commit, solve Vercel access first, bootstrap manager/admin routes into a real menu context when needed, ship the highest-leverage design and performance pass, and repeat until diminishing returns. Specify the target screen (e.g. /leroyslounge, /manager)."
