# Admin Design Loop Issues

## Current status (after pass 6)

No open issues remain from the initial `/admin` audit set.

## Resolved in this loop

1. Mobile shell no longer spends excessive first-screen space on an oversized rail slab.
2. `Return to menu view` now has clear contrast and button treatment in mobile light mode.
3. Mobile user-menu popover is anchored to the header zone and no longer collides with the first content card.
4. Desktop rail and main stage now align as a unified two-column console rather than detached stacked blocks.
